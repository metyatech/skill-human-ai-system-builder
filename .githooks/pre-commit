#!/bin/sh

if command -v pwsh > /dev/null 2>&1; then
  pwsh -File scripts/verify.ps1 || exit 1
fi

if command -v compose-agentsmd > /dev/null 2>&1; then
  compose-agentsmd --compose || exit 1
  git add AGENTS.md CLAUDE.md || exit 1
fi
